# Vite Demo

This demo aliases the local source code instead of consuming a packed tarball.

## Usage

```bash
npm install --prefix examples/vite-demo
npm run dev --prefix examples/vite-demo
```

The version endpoint is served from `/dist/version.json`.
Use `npm run bump --prefix examples/vite-demo` to simulate a new deployment.
